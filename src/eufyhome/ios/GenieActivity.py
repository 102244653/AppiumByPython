class GenieActivity(object):
    # 返回首页
    back_home = ('name', 'common icon back white')

    # 登录amazon
    sign_amazon = ('name', 'Sign in with Amazon')

    # 进入设置
    into_setting = ('name', 'common icon more')

    # 返回genie
    back_genie = ('name', 'common icon back')

    # 移除设备
    remove_device = ('name', 'Remove Device')

    # 确认移除
    commit_remove = ('name', 'Yes')

    # done
    done = ('name', 'Done')
