class GenieActivity(object):
    # 返回首页
    back_home = ('id', 'com.eufylife.smarthome:id/common_header_start_icon')

    # 登录amazon
    sign_amazon = ('id', 'com.eufylife.smarthome:id/sign_in_btn')

    # 进入设置
    into_setting = ('id', 'com.eufylife.smarthome:id/common_header_end_icon')

    # 返回genie
    back_genie = ('id', 'com.eufylife.smarthome:id/common_header_start_icon')

    # 移除设备
    remove_device = ('id', 'com.eufylife.smarthome:id/remove_device')

    # 确认移除
    commit_remove = ('xpath', '//*[@text="Delete"]')

    # done
    done = ('id', 'com.eufylife.smarthome:id/common_header_end_icon')
