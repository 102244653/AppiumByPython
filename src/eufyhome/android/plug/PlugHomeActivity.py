class PlugHomeActivity(object):
    # 返回
    back_home = ('id', 'com.eufylife.smarthome:id/common_header_start_icon')

    # 设置
    plug_setting = ('id', 'com.eufylife.smarthome:id/common_header_end_icon')

    # 设备名
    plug_name = ('id', 'com.eufylife.smarthome:id/title_tv')

    # 开关按钮
    plug_btn = ('id', 'com.eufylife.smarthome:id/switch_controller_cb')

    # 设备状态
    plug_status = ('id', 'com.eufylife.smarthome:id/switch_controller_text')

    # 定时任务
    schedules = ('id', 'com.eufylife.smarthome:id/schedules_tv')

    # 倒计时
    timer = ('id', 'com.eufylife.smarthome:id/timer_tv')

    # 耗电量
    energy = ('id', 'com.eufylife.smarthome:id/energy_usage_value')

    # 运行时间
    runtime = ('id', 'com.eufylife.smarthome:id/total_runtime_value')
    
    # ************************跳过引导************************** #
    # 跳过
    skip_guide = ('id', 'com.eufylife.smarthome:id/tv_skip')

    # 结束引导
    end_guide = ('id', 'com.eufylife.smarthome:id/done_btn')
